# next-auth v4 declares a peer dependency on next <=15; with Next 16 the strict
# peer-dep resolver (npm 7+) errors on install. legacy-peer-deps lets the install
# proceed. Remove this once next-auth v5 (Auth.js) is adopted.
legacy-peer-deps=true
